{
  "name": "webhook-proxy",
  "version": "1.0.0",
  "description": "Generic webhook proxy — validates, transforms and forwards events to OpenClaw",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
